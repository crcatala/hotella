# Guardrails

<!-- 
  Add project-specific rules that apply to EVERY iteration.
  Examples:
  - "Always run `npm test` before committing"
  - "Never modify files in /legacy without approval"
  - "Use snake_case for database columns"
  
  Unlike progress.md learnings, these are permanent constraints.
-->
